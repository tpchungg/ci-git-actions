apiVersion: apps/v1
kind: Deployment
metadata:
  name: py-app-deployment
  namespace: py-app
  labels:
    app: py-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: py-app
  template:
    metadata:
      labels:
        app: py-app
    spec:
      containers:
      - name: py-app-container
        image: tpchungg/py_app:81e57193def7531054576fdcc116a70cf54913a7
        imagePullPolicy: Always 
        ports:
        - containerPort: 8000 